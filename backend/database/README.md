# Backend Database (Mock)

This folder is intended to host database configuration and helpers for the backend API.

## Notes

- In production, you can replace this with a real database (PostgreSQL, Supabase, etc.)
- If you use Supabase, store the connection URL and keys in environment variables.
- For a quick demo, the `controllers` folder uses an in-memory list of publications.
